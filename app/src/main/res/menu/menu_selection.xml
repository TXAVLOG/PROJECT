<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_play_all"
        android:icon="@drawable/ic_play"
        android:title="@string/txamusic_play_selected"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_add_to_queue"
        android:icon="@drawable/ic_queue_music"
        android:title="@string/txamusic_add_to_queue"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_select_all"
        android:title="@string/txamusic_select_all"
        app:showAsAction="never"/>

</menu>
