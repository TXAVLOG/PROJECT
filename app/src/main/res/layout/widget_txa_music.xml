<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@drawable/widget_background"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <!-- Album Art -->
    <ImageView
        android:id="@+id/widget_album_art"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_default_album_art"
        android:background="@drawable/widget_album_art_bg"
        android:clipToOutline="true"
        android:contentDescription="@string/txamusic_album_art" />

    <!-- Content Container -->
    <LinearLayout
        android:id="@+id/widget_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <!-- Song Title -->
        <TextView
            android:id="@+id/widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="TXA Music"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="marquee"
            android:singleLine="true" />

        <!-- Artist Name -->
        <TextView
            android:id="@+id/widget_artist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tap to play"
            android:textColor="#AAAAAA"
            android:textSize="13sp"
            android:maxLines="1"
            android:layout_marginTop="2dp" />

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/widget_progress"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_marginTop="8dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/widget_progress_drawable" />

        <!-- Control Buttons -->
        <LinearLayout
            android:id="@+id/widget_controls"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Shuffle Button -->
            <ImageButton
                android:id="@+id/widget_btn_shuffle"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_shuffle_widget"
                android:scaleType="centerInside"
                android:contentDescription="@string/txamusic_shuffle" />

            <!-- Previous Button -->
            <ImageButton
                android:id="@+id/widget_btn_prev"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="4dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_skip_prev_widget"
                android:scaleType="centerInside"
                android:contentDescription="@string/txamusic_previous" />

            <!-- Play/Pause Button -->
            <ImageButton
                android:id="@+id/widget_btn_play_pause"
                android:layout_width="42dp"
                android:layout_height="42dp"
                android:layout_marginStart="4dp"
                android:background="@drawable/widget_play_button_bg"
                android:src="@drawable/ic_play_widget"
                android:scaleType="centerInside"
                android:contentDescription="@string/txamusic_play_pause" />

            <!-- Next Button -->
            <ImageButton
                android:id="@+id/widget_btn_next"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="4dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_skip_next_widget"
                android:scaleType="centerInside"
                android:contentDescription="@string/txamusic_next" />

            <!-- Repeat Button -->
            <ImageButton
                android:id="@+id/widget_btn_repeat"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="4dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_repeat_off_widget"
                android:scaleType="centerInside"
                android:contentDescription="@string/txamusic_repeat" />

        </LinearLayout>
    </LinearLayout>
</LinearLayout>
